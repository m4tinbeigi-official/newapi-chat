# Git Auto-Push Rule

Always check git status at the beginning of each turn or after editing/creating files. If there are any uncommitted changes, modified files, or untracked files, stage them, commit them with a descriptive message, and automatically push them to the current tracking branch (`origin main`).
Do not wait for the user to explicitly ask to push changes.
